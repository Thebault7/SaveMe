{% extends 'layout.html.twig' %}
{% form_theme registrationFormView 'bootstrap_4_layout.html.twig' %}
{% block title %}Nouveau compte{% endblock %}
{% block titre %}Créer un nouveau compte{% endblock %}
{% block body %}


    {{ form_start(registrationFormView) }}

    {{ form_label(registrationFormView.pseudo) }}
    {{ form_widget(registrationFormView.pseudo) }}
    {{ form_errors(registrationFormView.pseudo) }}

    {{ form_label(registrationFormView.nom) }}
    {{ form_widget(registrationFormView.nom) }}
    {{ form_errors(registrationFormView.nom) }}

    {{ form_label(registrationFormView.prenom) }}
    {{ form_widget(registrationFormView.prenom) }}
    {{ form_errors(registrationFormView.prenom) }}

    {{ form_label(registrationFormView.plainPassword) }}
    {{ form_widget(registrationFormView.plainPassword) }}
    {{ form_errors(registrationFormView.plainPassword) }}

        <button  class="btn btn-dark" style="margin-top: 20px">Enregistrer</button>
    {{ form_end(registrationFormView) }}
{% endblock %}
